# Google OAuth for viewer sign-in. Read by `supabase start` / `supabase status`.
# Copy to supabase/.env (gitignored) and fill in from Google Cloud Console:
#   APIs & Services -> Credentials -> OAuth client ID -> Web application
SUPABASE_AUTH_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_GOOGLE_SECRET=
